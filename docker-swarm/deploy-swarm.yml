---
- name: Prepare docker swarm nodes
  hosts: all
  roles:
    - create-sudo-user
    - update-all
    - init-user
    - install-git
    - install-modules
    - enable-firewalld

#FIXME, install docker
#FIXME, install docker plugins (i.e. Loki)
#FIXME, install docker manager and worker nodes
#FIXME, install monitoring stack with Grafana and Prometheus
