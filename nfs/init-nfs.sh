#!/bin/sh
#
# Script file to init nfs server. Typically, just create some directories to be shared by different applications.
#

# create directory "app" to be shared amoung applications
mkdir -p /mnt/nfs/app
